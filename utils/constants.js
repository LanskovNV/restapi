const entities = {
    employees: 'employees',
    users: 'users',
    tokens: 'tokens'
};

const authTokenTypes = {
    auth: 'auth',
    refresh: 'refresh'
}

module.exports = {
    entities,
    authTokenTypes
};
