const entities = {
    employees: 'employees',
    users: 'users',
};

const authTokenTypes = {
    auth: 'auth',
};

const pageSize = 5;

module.exports = {
    entities,
    authTokenTypes,
    pageSize,
};
